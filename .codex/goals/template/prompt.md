# Goal

Implement `<feature>`.

# Context

- Relevant crates:
- Relevant files:
- Existing interfaces:

# Constraints

- Rust stable only.
- Keep APIs testable and deterministic where possible.
- Preserve the Rust/Memgraph ownership boundary from the research reports.
- Follow `AGENTS.md`.

# Done When

- The relevant `cargo test` scope passes.
- `cargo clippy --workspace --all-targets -- -D warnings` passes when in scope.
- Documentation and goal artifacts are updated where behavior changes.
